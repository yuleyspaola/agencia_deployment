extends ../layout/index
block contenido
	main.container.mt-5
		.row
			.col-md-5
				img(src=`/img/destinos_${viaje.imagen}_ln.jpg`, class="img-fluid ")
			.col-md-7
				h1 #{pagina}
				p
				Fecha ida  #{viaje.fecha_id} - fecha vuelta : #{viaje.fecha_vuelta}
				p
				#{viaje.precio}
				p
				#{viaje.disponibles}
				P= viaje.descripcion



